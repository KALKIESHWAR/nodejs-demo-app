name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Debug secrets
        run: |
          echo "DOCKERHUB_USERNAME is set to: ${kalki145}"
          echo "DOCKERHUB_TOKEN is set to: ${kalki145}}"
          if [ -z "${kalki145}" ]; then echo "DOCKERHUB_USERNAME is empty"; exit 1; fi
          if [ -z "${kalki145}" ]; then echo "DOCKERHUB_TOKEN is empty"; exit 1; fi

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${kalki145}
          password: ${kalki145}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: KALKIESHWAR/nodejs-demo-app:latest